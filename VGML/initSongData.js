class SongData {
    constructor(
        title,
        link,
        series,
        game,
        song
    ) {
        this.title = title || "";
        this.link = link || "";
        this.series = series || "";
        this.game = game || "";
        this.song = song || "";
    }
}